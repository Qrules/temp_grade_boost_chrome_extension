chrome.browserAction.onClicked.addListener(function(tab) {
//maybe open a page to modify the grade setting?
//the other code was moved to editDOM.js, and that code will be injected into every page in the https://accesscenter.roundrockisd.org/* namespace
});
